#include <stdio.h>
int main(void)
{
    int in_word = 0; // in_word = 0 (FAUX);

    if (!45)
    {
        printf("Entre dans if\n"); 
    }
}

/*
int main(void)
{
    int in_word; // in_word = 0 (FAUX);

    if (in_word) // tester a enlever le !
    {
        printf("Entre dans if\n");
    }
}
*/